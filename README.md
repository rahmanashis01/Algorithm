# Algorithm

All source code for the lab task, lab report, and lab assessment.

Project structure:
- `src/` contains all `.c` source files.
- `output/` contains all compiled `.exe` files.

Example compile command:
```sh
gcc src/Problem_1.c -o output/Problem_1.exe
```
